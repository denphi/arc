"""ARC provider package.

Providers wrap LLM APIs (Anthropic, OpenAI, OpenWebUI…) and expose them
through `ProviderContract`. Shared utilities for parsing provider responses
live in `arc.providers.utils`.
"""
