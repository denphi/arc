# Local packages

*Develop a package outside the repo tree with `arc package init` / `validate`
and the `path` + `class` manifest style.*

The canonical local-packages design — goals, the manifest design, the loader
contract, the generated-agent contract, the CLI commands, the configuration
boundary, supported extension points, and error handling — follows.

```{include} ../../design/local-packages.md
```
